# Examples

This folder contains reference solutions demonstrating the expected file
format, comment structure, and test layout.

These files are excluded from CI test runs and from the solutions table in
the main README. They exist purely as a guide for what a finished solution
file should look like.

When you run `python scripts/new.py <number>`, the generated file will follow
this same structure with the problem-specific header and test cases pre-filled.
